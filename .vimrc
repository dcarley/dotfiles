" White on black is where it's at.
set bg=dark

" Auto-indent is more annoyance than aide.
set noai

" Stomp on hardtabs.
set expandtab
set shiftwidth=2
set softtabstop=2
set tabstop=2

if has("folding")
  " I want to love folding. But, no.
  set nofoldenable
endif
